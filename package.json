{
  "name": "relay",
  "version": "1.0.0",
  "description": "WebSocket relay server for Position-Independent-Agent — Cloudflare Workers + Durable Objects",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250306.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.4.0"
  }
}
